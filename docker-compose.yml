services:
  redis:
    image: redis:7-alpine

  api:
    build: .
    ports:
      - "8010:8010"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
    depends_on:
      - redis
